{
  "name": "notebooklm-omnibridge",
  "version": "2.0.0",
  "private": true,
  "workspaces": [
    "extension",
    "mcp-server",
    "core"
  ],
  "scripts": {
    "dev": "npm run dev -w extension",
    "build": "npm run build -w extension",
    "typecheck": "npm run typecheck -w extension",
    "mcp": "npm run start -w mcp-server"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "@types/node": "^20.19.39",
    "typescript": "^5.3.0"
  }
}
